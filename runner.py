
# Starting Point
# 1) Checks the agent being triggered (can we get this from the chron job via arguments?)
# 2) Pull repos from repo configs that have this agent configured (possibly just a bool on the config) 
    # need to verify any repos actually exist who want this agent run against them
# 3) For each repo
# 4) trigger the agent (e.g. issue scanner agent)


